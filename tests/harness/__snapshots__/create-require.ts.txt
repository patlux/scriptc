create-require.ts:11:13 - error SC2020: 'module.createRequire' is part of the standard library types but has no scriptc lowering yet

  10 | // The base names some OTHER file: unrecognized — the member fence.
  11 | const req = createRequire("/tmp/parent.js");
     |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  12 | console.log(String(req("./config.cjs")));

  hint: the lowered shape is a const binding over createRequire(import.meta.url) (or __filename) whose require calls take STATIC string literals — builtins, relative .json documents, and installed npm packages (under --dynamic) resolve at build time; dynamic specifiers cannot exist in a compiled binary's fixed module graph

create-require.ts:22:9 - error SC2020: 'createRequire's require of './helper.js'' is part of the standard library types but has no scriptc lowering yet

  21 | // A relative non-.json target: program modules are static imports.
  22 | require("./helper.js");
     |         ^~~~~~~~~~~~~
  23 | // Keep this as the final pointed fence in the diagnostic program.

  hint: relative requires lower for .json documents only — a program module is a static import