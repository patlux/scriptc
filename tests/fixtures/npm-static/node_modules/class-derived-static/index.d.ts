import { Foundation } from "class-base-static";

export declare class Formatter extends Foundation {
  derivedField: string;
  constructor(prefix: string);
  render(value: string): string;
  static create(prefix: string): Formatter;
}
