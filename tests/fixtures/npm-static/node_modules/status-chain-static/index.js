export class Loader {
  constructor(prefix) {
    this.prefix = prefix;
  }

  render(value) {
    return this.prefix + value;
  }
}

export class StatusIndicator extends Loader {
  render(value) {
    return super.render(value);
  }
}
