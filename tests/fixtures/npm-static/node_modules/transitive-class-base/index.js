var CompactBase = class {
  options = this.setOptions;

  setOptions(value) {
    return `base:${value}`;
  }

  render(value) {
    return `base:${value}`;
  }
};

const unsupportedDynamic = (name) => import(name);

export { CompactBase, unsupportedDynamic };
