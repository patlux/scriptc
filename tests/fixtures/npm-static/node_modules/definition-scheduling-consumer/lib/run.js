import { getRegistry } from "definition-scheduling-provider";

function read(value) {
  if (getRegistry().has(value)) return getRegistry().get(value);
  return value;
}

export function run(value) {
  return read(value);
}
