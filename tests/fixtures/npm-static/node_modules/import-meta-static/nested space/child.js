import { fileURLToPath } from "node:url";

export const nested = String(
  import.meta.url.includes("nested%20space/child.js") &&
  fileURLToPath(import.meta.url).endsWith("/nested space/child.js"),
);
