const match = {
  0: function () { return this === match ? "zero" : "bad-this"; },
  1: function (value) { return this === match ? `one:${value}` : "bad-this"; },
};

exports.matchArity = function (...args) {
  return match[args.length]?.(...args);
};
