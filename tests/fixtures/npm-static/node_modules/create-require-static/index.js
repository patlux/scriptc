import { createRequire } from "node:module";

const require = createRequire(import.meta.url);
const path = require("node:path");
require("./data.json");

export const value = path.basename("/tmp/static.txt");
