export interface PackageLayout {
  packageDir: string;
  candidates: string[];
}

export function createEntrypointPackageLayout(
  entrypoint: string | undefined,
  cwd?: string,
  pathValue?: string | undefined,
): PackageLayout;
