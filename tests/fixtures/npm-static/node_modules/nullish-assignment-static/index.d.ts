export interface Extension<T> {
  entryRenderers?: Map<string, T>;
}

export declare function register<T>(extension: Extension<T>, value: T): void;

export interface PackageLayout {
  packageDir: string;
}

export declare function applyPackageLayoutEnvironment(
  layout: PackageLayout,
  environment?: Record<string, string | undefined>,
): void;
