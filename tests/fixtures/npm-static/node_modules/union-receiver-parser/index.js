exports.parseFrontmatter = function parseFrontmatter(content) {
  if (content === "bad") {
    return { ok: false, error: new Error("boom") };
  }
  return { ok: true, value: { frontmatter: {}, body: content } };
};
