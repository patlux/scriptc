export interface Provider {
  enabled: boolean;
  id: string;
  options: { attempts: number; tags: string[] };
}

export function registerProvider(id: string, provider: Provider): void;
export function lookupProvider(id: string): Provider | undefined;
export function requiredProvider(id: string): Provider;
export function hasProvider(id: string): boolean;
